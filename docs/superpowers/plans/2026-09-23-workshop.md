# Workshop implementation plan

Goal: implement codex_workshop_pack/01_CODEX_MASTER_PROMPT.md on test only.
Architecture: standalone protected route, existing inventory providers, a pure layout model,
procedural Three scene using the prototype's instancing and camera patterns, normalized SQL
with atomic snapshot RPC and scoped local fallback. Existing prototype remains available.
No new dependencies, push, remote migration or synthetic inventory/telemetry.

- [x] Model and tests: rooms, furniture, stable local slots, rotation, bounds/collision,
  placement uniqueness, cascade removal, cache validation.
- [x] Scene: reuse instanceTemplate/disposal and demand rendering, two GLBs, shared spool,
  room/table/rack geometry, selection, smooth focus, floor dragging, minimap.
- [x] UI: protected /workshop and navbar, shared controls, room/furniture editors,
  existing inventory placement, cross-room search, responsive inspector.
- [x] Persistence: normalized tables/RLS, atomic save/get RPC with revision check,
  local cache namespaced by user, recoverable offline changes. SQL verified on isolated PostgreSQL; remote migration not applied.
- [x] Assets: reference-derived GLBs generated with installed Three.js; standalone Blender generator and metrics.
- [x] Verify npm test, browser workflows and visual comparison, npm run build.
  Do not run full e2e suite without push request. Record limitations and file inventory.

