# Database

Не применяй SQL вслепую. `04_DATABASE_PLAN.md` — план. Codex должен сначала прочитать актуальный `supabase_schema.sql` ветки `test`, определить семантику `printers` и только затем написать migration под реальный проект.
